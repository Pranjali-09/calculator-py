# 🧮 Simple Command-Line Calculator (Beginner Friendly)

This is a **beginner-level Python 3** command-line calculator that performs basic arithmetic operations.  
It uses **no functions**, **no classes**, and **no try/except** blocks — making it ideal for new programmers learning basics.

---

## 🚀 Features

The calculator supports:

1. **Addition**
2. **Subtraction**
3. **Division**
4. **Multiplication**
5. **Square (x²)**
6. **Square Root**
7. **Exit option**

Everything runs inside a simple `while` loop that repeats until the user selects `0`.

---

## 🧼 Code Characteristics

- No GUI (command-line only)
- Very beginner-friendly
- Uses the Python `math` module for square root
- No error handling (invalid input will crash)
- Minimal and easy-to-read logic

---

## 📦 Requirements

- **Python 3.6 or later**
- No external libraries

---

## ▶️ How to Run

1. Save the script as:
2. Open a terminal or command prompt
3. Run the file:

```bash
python3 main.py
